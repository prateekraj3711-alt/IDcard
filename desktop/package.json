{
  "name": "stark-id-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri desktop wrapper for the Stark Industries ID admin portal.",
  "scripts": {
    "build:web": "cd ../dashboard && npm run build",
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "npm run build:web && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6.3"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.6.0"
  }
}
