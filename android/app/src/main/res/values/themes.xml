<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Post-splash theme — Compose owns rendering after this point. -->
    <style name="Theme.IdCard" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/brand_surface</item>
    </style>

    <!-- Android 12+ SplashScreen. Backported to older releases by
         androidx.core:core-splashscreen. Solid brand background with the
         white "SI" brandmark; hands off to Theme.IdCard once Compose mounts. -->
    <style name="Theme.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/brand_primary</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/brand_primary</item>
        <item name="postSplashScreenTheme">@style/Theme.IdCard</item>
    </style>
</resources>
